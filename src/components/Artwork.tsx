import { useEffect, useRef } from 'react'
import { ArtworkProps } from '../types'

// Component for drawing seamless background art down the page
function Artwork ({ 
  color = 'white',
  speed = 1,
  ...props
}: ArtworkProps) {
  
}

export default Artwork